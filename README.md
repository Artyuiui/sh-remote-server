# sh-remote-server
## 🚀 Installation (ติดตั้งแบบง่ายและรวดเร็ว)

ระบบนี้คือเครื่องมือสร้าง **SSH คีย์ลัด (`sh <name>`)**  
ช่วยให้เข้า SSH ได้สั้น จำง่าย และจัดการ remote ได้สะดวก

รองรับ:
- Linux
- macOS
- Windows (WSL)

> ใช้ `ssh` มาตรฐาน ปลอดภัย ไม่แก้ระบบหลัก

---

### 📦 ความต้องการเบื้องต้น
- มีคำสั่ง `ssh` อยู่แล้ว (ส่วนใหญ่มีมาให้)
- ใช้ shell แบบ `bash` หรือ `zsh`

ตรวจสอบ:
```bash
ssh -V
